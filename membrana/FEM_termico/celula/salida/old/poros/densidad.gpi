set terminal png

set xlabel "Angulo [º]"
set ylabel "Densidad [1 / um**2]"

set xrange [0 : 180]

set output "densidad.png"

plot "dens.csv" using (($1) * 57.2957795):($2) title "Densidad en 200ms" with points, \
