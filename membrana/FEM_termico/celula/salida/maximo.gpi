set terminal png

set xlabel "Tiempo us"
set ylabel "radio"

set xrange [0:100]

set output "maximo.png"

plot "maximo.dat" using ($1 * 1e6):($2) with lines title "d", \
	 "maximo.dat" using ($1 * 1e6):($3) with lines title "db", \
	 "maximo.dat" using ($1 * 1e6):($4) with lines title "hb", \
	 "maximo.dat" using ($1 * 1e6):($5) with lines title "h", \
